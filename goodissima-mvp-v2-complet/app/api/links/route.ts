import { NextResponse } from "next/server"; import { prisma } from "@/lib/prisma"; import { slugify } from "@/lib/slug"; import { auditLog } from "@/lib/audit";
const DEMO_OWNER_EMAIL="demo@goodissima.app";
export async function POST(req:Request){const body=await req.json(); if(!body.title||typeof body.title!=="string") return NextResponse.json({error:"Title required"},{status:400}); const owner=await prisma.user.upsert({where:{email:DEMO_OWNER_EMAIL},update:{},create:{email:DEMO_OWNER_EMAIL,name:"Propriétaire Démo"}}); const slug=`${slugify(body.title)}-${Math.random().toString(36).slice(2,7)}`; const link=await prisma.gLink.create({data:{ownerId:owner.id,slug,title:body.title,city:body.city||null,description:body.description||null,rules:{requireEmail:Boolean(body.requireEmail),requireMessage:Boolean(body.requireMessage),allowDocument:Boolean(body.allowDocument)}}}); await auditLog({actorEmail:owner.email,eventType:"LINK_CREATED",metadata:{gLinkId:link.id,slug:link.slug}}); return NextResponse.json(link);}
