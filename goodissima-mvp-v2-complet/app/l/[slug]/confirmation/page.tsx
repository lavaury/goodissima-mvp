import Link from "next/link";
export default function ConfirmationPage({params,searchParams}:{params:{slug:string};searchParams:{caseId?:string;candidate?:string}}){const caseUrl=searchParams.caseId?`/cases/${searchParams.caseId}?candidate=${encodeURIComponent(searchParams.candidate??"")}`:`/l/${params.slug}`; return <main className="mx-auto flex min-h-screen max-w-2xl flex-col justify-center px-6"><div className="rounded-3xl border bg-white p-8 text-center shadow-sm"><p className="text-sm font-semibold uppercase tracking-wide text-slate-500">Goodissima — Relation sécurisée</p><div className="mx-auto mt-6 flex h-16 w-16 items-center justify-center rounded-full bg-slate-100 text-3xl">✓</div><h1 className="mt-6 text-3xl font-bold">Votre demande a été envoyée</h1><p className="mt-4 text-slate-600">Le propriétaire va pouvoir vous répondre dans ce dossier sécurisé. Vous pouvez revenir à cet espace à tout moment.</p><div className="mt-8 flex flex-col justify-center gap-3 sm:flex-row"><Link href={caseUrl} className="rounded-2xl bg-slate-900 px-5 py-3 text-white">Ouvrir ma conversation</Link><Link href={`/l/${params.slug}`} className="rounded-2xl border px-5 py-3">Revenir à l’annonce</Link></div></div></main>}
