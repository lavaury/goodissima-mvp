export const dynamic = "force-dynamic";import Link from "next/link"; import { prisma } from "@/lib/prisma"; import { StatusBadge } from "@/components/StatusBadge";
export default async function CasesPage(){const cases=await prisma.relationCase.findMany({include:{gLink:true,messages:{orderBy:{createdAt:"desc"},take:1}},orderBy:{createdAt:"desc"}}); return <main className="mx-auto max-w-6xl px-6 py-10"><h1 className="text-3xl font-bold">Dossiers relationnels</h1><div className="mt-8 overflow-hidden rounded-2xl border bg-white">{cases.length===0?<p className="p-8 text-slate-500">Aucun dossier pour le moment.</p>:cases.map((item)=><Link key={item.id} href={`/cases/${item.id}`} className="block border-b p-5 hover:bg-slate-50"><div className="flex items-center justify-between gap-4"><div><p className="font-semibold">{item.candidateName}</p><p className="text-sm text-slate-500">{item.gLink.title} · {item.candidateEmail}</p></div><StatusBadge status={item.status}/></div></Link>)}</div></main>}
