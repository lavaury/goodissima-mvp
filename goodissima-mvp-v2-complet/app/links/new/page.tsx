"use client";
import { useState } from "react"; import { useRouter } from "next/navigation";
export default function NewLinkPage(){const router=useRouter(); const [form,setForm]=useState({title:"",city:"",description:"",requireEmail:true,requireMessage:true,allowDocument:true}); async function submit(){if(!form.title.trim()){alert("Merci de saisir un titre d'annonce.");return;} const res=await fetch("/api/links",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)}); if(!res.ok){alert("Erreur lors de la création du lien.");return;} const link=await res.json(); router.push(`/l/${link.slug}`);} return <main className="mx-auto max-w-2xl px-6 py-10"><h1 className="text-3xl font-bold">Créer un lien sécurisé</h1><div className="mt-8 space-y-4 rounded-2xl border bg-white p-6"><input className="w-full rounded-xl border px-4 py-3" placeholder="Titre de l'annonce" value={form.title} onChange={(e)=>setForm({...form,title:e.target.value})}/><input className="w-full rounded-xl border px-4 py-3" placeholder="Ville" value={form.city} onChange={(e)=>setForm({...form,city:e.target.value})}/><textarea className="min-h-32 w-full rounded-xl border px-4 py-3" placeholder="Description courte" value={form.description} onChange={(e)=>setForm({...form,description:e.target.value})}/><label className="flex gap-2"><input type="checkbox" checked={form.requireEmail} onChange={(e)=>setForm({...form,requireEmail:e.target.checked})}/> Email obligatoire</label><label className="flex gap-2"><input type="checkbox" checked={form.requireMessage} onChange={(e)=>setForm({...form,requireMessage:e.target.checked})}/> Message obligatoire</label><label className="flex gap-2"><input type="checkbox" checked={form.allowDocument} onChange={(e)=>setForm({...form,allowDocument:e.target.checked})}/> Document optionnel</label><button onClick={submit} className="w-full rounded-2xl bg-slate-900 px-5 py-3 text-white">Générer le lien</button></div></main>}
