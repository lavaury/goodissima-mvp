# Goodissima production / Vercel Production
# Never commit real secrets in this file.

GOODISSIMA_ENV=production
GOODISSIMA_DEBUG_MODE=false

NEXT_PUBLIC_APP_URL=https://app.goodissima.app

# Supabase production PostgreSQL for Prisma server-side access only.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/postgres?sslmode=require"

# Used by Prisma migrations / direct connections if needed.
DIRECT_URL="postgresql://USER:PASSWORD@HOST:PORT/postgres?sslmode=require"

# Supabase server URL for future server-side use.
SUPABASE_URL="https://PRODUCTION_PROJECT_REF.supabase.co"

# Supabase browser auth client. RLS must prevent direct table access.
NEXT_PUBLIC_SUPABASE_URL="https://PRODUCTION_PROJECT_REF.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="PRODUCTION_SUPABASE_ANON_KEY"

# Server-side Supabase admin operations only.
SUPABASE_SERVICE_ROLE_KEY="PRODUCTION_SUPABASE_SERVICE_ROLE_KEY"

# Email provider.
RESEND_API_KEY=""
EMAIL_FROM="Goodissima <notifications@goodissima.app>"

# Production AI remains deterministic for the initial launch.
AI_PROVIDER=mock
AI_TEST_MODE=scenario
AI_EMBEDDING_PROVIDER=mock
MISTRAL_API_KEY=""
MISTRAL_MODEL=mistral-small-latest

# Embedding worker controls.
EMBEDDING_JOB_BATCH_SIZE=10
EMBEDDING_WORKER_FORCE_FAIL=0

# Feature flags.
FEATURE_AI=true
FEATURE_MATCHING=true
FEATURE_ANALYTICS=true
FEATURE_EXPERIMENTAL=false
